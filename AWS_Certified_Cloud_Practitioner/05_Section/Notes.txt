        Section 5 EC2 Elastic Compute Cloud

AWS Budget Setup
    Billing and Cost management 
        Must be run from root account
        IAM user and role access to Billing Information
            This will allow user to access billing info
        Free Tier lets you see services that are free but might cost you money eventually
        Budgets -> create a budget that will alert you when you hit a threshold
            Template - Zero spend budget will notify you when you are going to spend 0.01 cents 

EC2 Basics
    Amazon EC2 
        EC2 is one of the most popular AWS offering
        EC2 = Elastic Compute Cloud = Infrastructure as a Service IaaS
        Consists in the capability of:
            Renting virtual machines (EC2)
            Storing data on virtual drives (EBS)
            Distributing load acrss machines (ELB)
            Scaling the services using an auto-scaling-group (ASG)
        Knowing EC2 is fundamental to understand how the cloud works
    EC2 Sizing & configuration options 
        OS choices: Linux, Windows, Mac OS 
        How much compute power and cores (CPU)
        How much random access memory (RAM)
        How much storage space:
            Network attached (EBS and EFS)
            Hardware (EC2 Instance Store)
        Network Card: speed of the card , public IP address
        Firewall rules: security gap
        Bootstrap script (configure at first launch): EC2  User Data 
    EC2 User Data:
        It is possible to bootstrap our instances using an EC2 User data script
        Bootstrapping mean launching commands when a machine starts
        That script is only run once at the instance first start 
        EC2 user data is used to automate boot tasks such as:
            Installing updates
            Installing software
            Downloading common files from the internet 
            Anything you can think of 
        The EC2 User Data script runs with the root user. (runs with sudo)

Create an EC2 Instance
    Launch first virtual server running Linux 
    Launch web server on EC2 instance 
    Start/Stop instance 
        EC2 Console -> instances -> launch instance 
            Name -> My first instance 
            Choose OS or base image (Amazon Linux 2 AMI)
            Instance type -> cpu, memory, cost (t2 micro free tier) 
            key pair -> to login to instance ssh (EC2 tutorial) RSA
            Network settings -> public IP enable 
            Security Group (firewall)-> Add security rules
                allow ssh traffic anywhere 
                allow http traffic from the internet (webserver)
            Storage 8GiB -> gp2 root volume 
                delete on termination -> once we delete instance vlume will be deleted 
            Advanced details
                User data info -> pass script to execute on firts launch (bash scripting)
            Summary -> start 1 instance 
            Launch the instance 
        If you stop an instance and then start the public IP maybe changed 
    To use SSH key
        Download the ssh key for the instance
        Put the key in the /home/.ssh/ folder
        Run the command:
            ssh -i ~/.ssh/<key_file_name> <user-name (ec2-user)>@<public-ip>

EC2 Instance Types Basics
    You can use different types of EC2 instances that are optimized for differernt use cases:
    AWS has the name convention: m5.2XLarge
        m: instance class (general purpose)
        5: generation (AWS improves them over time)
        2xl : size within the instance class     
    Types:
        General purpose 
            Great for diversity of workloads (webservers, code repo)
                Balance between compute, memory, networking (t2)
        Compute Optimized (c5,c6)
            Great for compute intensive tasks that require high performance processors
                Ex: batch processing, media transcoding, high performance web server, machine learning, gaming servers
        Memory (RAM) Optimized (r , x)
            Fast performance for workloads that process large data set in memory
            Use cases: High performance database, Distributed web scale cache stores, In-memory database, rt-processing of big data  
        Storage Optimized 
            Great for storage intensive tasks that require high sequential read and write access to large data sets on local storage
                Use cases: High freq online transaction, Distributed file-systems, Data warehousing applications 

Security Groups & Classic Ports Overview 
    Introduction to Security Groups (Firewall)
        Security groups are the fundamental of network security in AWS
        They control how traffic is allowed into or out of our EC2 instances
            www ----> [Sec Group [EC2 Instance]  ]
                We want to access EC2 instance from home (www)
                We create a securtiy group around EC2 instance
                    Securtiy group has rule on inbound and outbound traffic 

        Security groups only conatain allow rules 
        Security groups rules can reference by IP or by security group 
    Security Group Deeper Dive 
        Act as firewall on EC2 instances 
        They regulate:
            Access Ports
            Authorized IP ranges IPv4 and IPv6 
            Control of inbound network (from other to the instance)
            Control of outbound network (from the instance to other)
    Good to Know 
        Can be attached to multiple instances 
        Locked down to a region /VPC combination 
        The security group lives outside the EC2 Instance     --->{Sec Group   [EC2 instance] }<----
            If the traffic is blocked the EC2 instance wont see it 
        Its good to maintain one seperate securtiy group for SSH access 
        If your application is not accessible, its a sec group issue
        If your application gives a "connection refused" error then its an application error or its not launched
        All inbound traffic is blocked by default 
        All outbound traffic is authorized by default 
    Classic Ports to Know
        22 =  SSH (Secure Shell) log into a Linux instance 
        21 = FTP (File Transfer Protocol) upload files into a file share 
        22 = SFTP (Secure File Transfer Protocol) upload files using ssh 
        80 = HTTP access unsecured websites 
        443 = HTTPS access secured websites 
        3389 = RDP (Remote Desktop Protocol) - log into a Windows instance 

Securtiy Groups Hands On
    On the left tab go to Networking and Security Groups 

SSH Overview 
    EC2 Instance connect can remote into an instance from web browser
        Platform agnostic 

SSH on Linux
    Change permissions of the key.pem file  
        chmod 0400 key.pem 
    ssh -i <pathOfKey>/<key_name>.pem ec2-user@<ec2_ip>
        You are now logged into the instance 
    To exit
        exit or Ctrl-d 

SSH Troubleshooting 

EC2 Instance Connect
    While in the instance window 
        Click connect 
        Use default EC2 Instance Connect option 

EC2 Instance Roles Demo
    Never enter your access ID or key into an EC2 instance 
        Someone could remote into the instance and then retrieve your info 

EC2 Instance Purchasing Options
    On-Demand Instances -> short workload, predictable pricing, pay by second
    Reserved (1&3 years)
        Reserved instances - long workloads 
        Convertible reserved instances - long workloads with flexible instances
    Savings Plans (1&3 years) - commitment to an amount of usage, long workload 
    Spot Instances - short workloads, cheap, can lose instances (less reliable)
    Dedicated Hosts - book an entire physical server, control instance placement
    Dedicated Instances - no other customers will share our hardware

    EC2 On Demand
        Pay for what you use:
            Linux or Windows - billing per second after first minute 
            All other operating systems - billing per hour
        Has the highest cost but no upfront payment 
        No long term commitment 
        Recommended for short term and un-interrupted workloads

    EC2 Reserved Instances
        Up to 72% discount compared to on demand 
        You reserve a specific instance attributes (Instance type, Region, Tenancy, OS)
        Reservation period  1 yr or 3 yr 
        Payment options - no upfront, partial upfront, all upfront
        Reserved Instances Scope - Regional or Zonal (reserve capacity in an AZ)
        Recommended for steady state usage appications (database)
        You can buy and sell in the Reserved Instance Marketplace 

        Convertible Reserved Instance 
            Can change instance type, family, OS 

    EC2 Savings Plan 
        Get a discount based on long-term usage 
        Commit to a cerain type of usage ($10/hour for 1/3 years)
        Usage beyond the EC2 Savings plan is billed at the On-Demand price 
        Locked to a specific instance family and AWS region  (M5 in use-east-1)
        Flexible across:
            Instance size (m5.xlarge, m5.exlarge)
            OS (Linux, Windows)
            Tenancy (Host, dedicated, default)

    EC2 Spot Instances
        Most aggressive discount 
        Instances that you can lose at any point of time if your max price is less than the current spot price 
        The most cost efficient 
        Useful workloads:
            batch jobs
            data analysis 
            image processing 
            flexible start and end times
        Not suitable for critical jobs 

    Dedicated Hosts 
        A physical server with EC2 instance capacity fully dedicated to your use 
        Allows you address compliance requirements and use your existing server-bound sw licenses (per-socket,per-core,per- VM sv lic)
        Purchasing options:
            On-demand pay per second for active Dedicated Hosts
            Reserved 1 or 3 years (no upfront, partial upfront, all )
        The most expensive 
        Useful for sw that have complicated licensing model 
        Or for companies that have strong regulatory or compliance needs 
        
    EC2 Dedicated Instances
        Instances run on hw thats dedicated to you
        May share hw with other instances in same account 
        No control over instance placement (can move hw after start/stop)
        
    EC2 Capacity Reservations
        Reserve On-demand instance capacity in a specific AZ for any duration 
        You always have access to EC2 capacity when you need it 
        No time commitment (create/cancel anytime) no billing disount 
        Combine with regional reserved instances and savings plans to benefit from billing discounts 
        You are charded on-demand rate whether you run instances or not 
        Suitable for short term uninterrupted workloads that needs to be in a specific AZ 

    Which purchasing option is right for me? (Hotel analogy)
        ON demand: stay in resort whenever we like, pay full price
        Reserved: plan ahead and if we plan to stay for a long time, we may get a good discount 
        Savings Plan: Pay a certain amount per hour for certian period and stay in any room type 
        Spot Instances: hotel allows people to bid for the empty rooms and the highest bidder keeps the rooms. kicked out at any time 
        Dedicated Hosts: We book an entire building of the resort 
        Capacity Reservations: you book a room for a period with full price even if you dont stay in it 

Shared Responsibility Model for EC2
    AWS
        Infrastructure (global network security)
        Isolation on physical hosts 
        Replacing faulty hardware 
        Compliance validation
    You
        Security groups rules
        Operating sytem patches and updates 
        Software and utilities installed on teh EC2 instance 
        IAM Roles assigned to EC2  & IAM user access management 
        Data security on your instance

EC2 Summary 
    EC2 instances
        AMI(OS) + Instance size (CPU + RAM) + Storage + security groups + EC2 User data 
    Security groups: Firewall attached to EC2 instance 
    EC2 User Data: Script launched at the first start of an instance 
    SSH : start a terminal into our EC2 instance
    EC2 Instance Role: Link to IAM roles 
    Purchasing Options: On-Demand, Spot, Reserved (Standard + Concertible), Dedicated host, Dedicated instance 
    